# CHANGELOG

รูปแบบ: [Keep a Changelog](https://keepachangelog.com/) แบบย่อ — ทุกครั้งที่แก้ไขโปรเจกต์ ให้เพิ่มหัวข้อใหม่ด้านบนสุดพร้อมวันที่

## [1.4.0] - 2026-08-09

### เพิ่ม (Added)
- **เริ่มบิลได้โดยไม่ต้องสแกน**: แท็บแรก (เดิมชื่อ "สแกน" เปลี่ยนเป็น "เริ่มต้น") ตอนนี้แสดงตัวเลือกชัดเจน 2 ทาง — "สแกนใบเสร็จ" หรือ "กรอกเอง" — กดปุ่ม "เริ่มกรอกรายการเอง" จะพาไปแท็บรายการพร้อมช่องกรอกเปล่าให้ทันที ไม่ต้องมีใบเสร็จเลยก็ใช้แอปได้
- คำแนะนำใน `README.md` เรื่องวิธีเปิดไฟล์ `index.html` ให้ถูกต้อง (ต้องแตกไฟล์ zip ก่อน ห้ามเปิดจากข้างในหน้าต่าง ZIP ตรง ๆ) พร้อมอธิบายเหตุผลทางเทคนิคว่าทำไม PWA ถึงใช้งานไม่ได้ถ้าเปิดผ่าน `file://`
- คำแนะนำการฝากไฟล์ขึ้นเว็บแบบละเอียดใน `README.md` เปรียบเทียบ **GitHub Pages** (แนะนำ) กับ **Firebase Hosting** (ตัวเลือกรอง) พร้อมคำสั่ง git ที่พร้อมรันได้ทันที

### แก้ไข (Fixed)
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v1` → `v2`) เพื่อให้ผู้ใช้ที่ติดตั้ง PWA ไว้แล้วได้รับโค้ดเวอร์ชันล่าสุด ไม่ติดแคชเก่าค้าง
- ลบ element `#scanIdleHint` ที่ไม่จำเป็นแล้ว (ถูกแทนที่ด้วย UI ตัวเลือกเริ่มต้นที่ชัดเจนกว่า)

### หมายเหตุสำคัญ
- ปัญหา "เว็บไม่มีสไตล์เลย เป็นตัวหนังสือดำล้วน" ที่พบตอนเปิดไฟล์จากข้างใน ZIP **ไม่ใช่บั๊กของโค้ด** แต่เกิดจาก Windows แตกไฟล์ `index.html`ออกมาไฟล์เดียวโดยไม่มีโฟลเดอร์ `css/`/`js/` ติดมาด้วย วิธีแก้คือแตกไฟล์ zip ให้ครบก่อนเปิด (ดู README) หรือฝากขึ้นเว็บโฮสติ้งแล้วเปิดผ่านลิงก์แทน

## [1.3.0] - 2026-08-09

### เพิ่ม (Added)
- ชุดเทสต์ครบทั้งโปรเจกต์ในโฟลเดอร์ `tests/` (35 เทสต์ / 10 กลุ่ม) รันด้วย `npm install && npm test` โดยใช้ Node.js built-in test runner (`node:test`) ไม่ต้องพึ่ง framework ทดสอบภายนอก:
  - `splitter.test.js` — ยูนิตเทสต์การคำนวณ (VAT 2 โหมด, ค่าบริการเปิด/ปิด, edge case ต่าง ๆ)
  - `ocr.test.js` — ยูนิตเทสต์ heuristic แปลง OCR เป็นรายการ
  - `storage.test.js` — ยูนิตเทสต์ localStorage และการ migrate ข้อมูลเก่า
  - `integration.test.js` — เทสต์ end-to-end ที่รัน **โค้ดจริงของแอป** บน DOM จำลอง (jsdom) จากไฟล์ `index.html` ตัวจริง จำลองผู้ใช้ทำงานครบวงจร (สแกน → รายการ → คนกิน → VAT/ค่าบริการ → หารบิล → เริ่มบิลใหม่) พิสูจน์ว่าใช้งานได้จริง ไม่ใช่แค่ทดสอบฟังก์ชันแยกส่วน
- `package.json` (สำหรับนักพัฒนาเท่านั้น ไม่จำเป็นสำหรับผู้ใช้ทั่วไป) พร้อม devDependency `jsdom` สำหรับรันเทสต์ end-to-end

### แก้ไข (Fixed) — บั๊กจริงที่เทสต์ชุดนี้ตรวจพบ
- **`js/ocr.js`**: regex จับราคาเดิม (`[0-9]{1,3}...`) จับตัวเลขได้แค่ 3 หลักท้ายเมื่อราคาไม่มีคอมมาแบ่งหลักและยาวเกิน 3 หลัก (เช่น "999999" จะถูกจับเป็น "999") ทำให้ตัวกรอง "ราคาไม่สมเหตุสมผล (>100,000)" หลุดไป แก้เป็น `[0-9]+...` เพื่อจับตัวเลขทั้งจำนวนก่อนกรอง
- **`tests/integration.test.js`**: Node.js เวอร์ชันใหม่มี `global.navigator` เป็น getter-only (สำหรับ fetch API) การ assign ตรง ๆ ทำให้ throw ต้องใช้ `Object.defineProperty` แทน

## [1.2.0] - 2026-08-09

### เพิ่ม (Added)
- ตัวเลือก **VAT**: เปิด/ปิดได้ (สวิตช์), เมื่อเปิดแล้วเลือกได้ว่า "ราคายังไม่รวม VAT" (ระบบบวก VAT เพิ่มให้) หรือ "ราคารวม VAT แล้ว" (ระบบแยกยอด VAT ที่แอบซ่อนอยู่ในราคาให้ดู โดยไม่บวกซ้ำ) พร้อมกำหนดอัตราภาษีเองได้ (ค่าเริ่มต้น 7%)
- ตัวเลือก **ค่าบริการ (Service charge)**: เปิด/ปิดได้ (สวิตช์), เมื่อเปิดแล้วกำหนดอัตราค่าบริการเองได้ (ค่าเริ่มต้น 10%)
- ป้ายกำกับในกล่องสรุปยอด (totals box) ปรับข้อความอัตโนมัติตามโหมด VAT ที่เลือก เช่น "รวมรายการ (รวม VAT แล้ว)" หรือ "VAT ที่รวมอยู่ในราคาแล้ว"
- ระบบ migrate ข้อมูลบิลเก่า (ที่บันทึกไว้ก่อนมีตัวเลือกนี้) ให้เข้ากับโครงสร้างใหม่โดยอัตโนมัติ ไม่ทำให้ข้อมูลเดิมพัง

### เปลี่ยนแปลง (Changed)
- `calcBillTotals()` ใน `js/splitter.js` คำนวณแยกกรณี "ราคารวม VAT แล้ว" โดยแยกฐานราคาก่อนภาษีออกมาก่อนคิดค่าบริการ (แทนที่จะบวก VAT ทับซ้ำ)
- `splitItemized()` เปลี่ยนมาคำนวณส่วนค่าบริการ/ภาษีที่หารตามสัดส่วนจาก `grandTotal - subtotal` แทนการบวก serviceAmount+vatAmount ตรง ๆ เพื่อให้ถูกต้องไม่ว่าจะเลือกโหมด VAT แบบไหนก็ตาม

## [1.1.0] - 2026-08-09

### เปลี่ยนแปลง (Changed)
- รีธีมทั้งเว็บเป็นสีครีม-ส้ม-ขาว (cream/orange/white) แทนธีมกระดาษใบเสร็จสีเข้มแบบเดิม เปลี่ยนฟอนต์หัวข้อเป็น Baloo 2 (ตัวกลม อ่านง่าย เป็นกันเอง)
- ปรับแท็บเมนูให้มีไอคอนและเลขขั้นตอน (1-4) กำกับ เพื่อให้เห็นลำดับการใช้งานชัดเจนขึ้น
- เปลี่ยนตารางรายการอาหาร (ที่แน่นและกดยากบนมือถือ) เป็นการ์ดรายการแบบ list พร้อมปุ่ม +/- ปรับจำนวนแบบแตะง่าย ไม่ต้องพิมพ์เลข
- เพิ่มวงกลมอักษรย่อ (avatar) ให้แต่ละคนในหน้ารายชื่อ หน้าระบุคนกิน และหน้าสรุปผลหารบิล ช่วยให้แยกคนได้ง่ายขึ้นด้วยสายตา
- ปรับปุ่มทั้งหมดเป็นทรงพิลล์ (rounded) และขยายพื้นที่กดให้ใหญ่ขึ้นสำหรับใช้งานบนมือถือ
- เพิ่ม empty state แบบมีอิโมจิประกอบและคำแนะนำที่ชัดเจนขึ้นในทุกแท็บที่ยังไม่มีข้อมูล

### แก้ไข (Fixed)
- อัปเดตตัวเลือกใน `js/app.js`/`js/ui.js` ให้ตรงกับโครงสร้าง DOM ใหม่ (การ์ดรายการแทนตาราง) พร้อมทดสอบว่าไม่มี id/selector หลุดอ้างอิงผิด

## [1.0.0] - 2026-08-09

### เพิ่ม (Added)
- โครงสร้างเว็บแอปเริ่มต้นทั้งหมด: สแกนใบเสร็จ, จัดการรายการ, จัดการหมวดหมู่, จัดการรายชื่อคน, หารบิล
- ระบบสแกนใบเสร็จด้วย Tesseract.js (OCR ทำงานในเบราว์เซอร์ ไม่ส่งรูปขึ้นเซิร์ฟเวอร์) พร้อม heuristic แปลงข้อความเป็นรายการอาหาร (`js/ocr.js`)
- การจัดการรายการอาหาร: เพิ่ม/ลบ/แก้ไขชื่อ จำนวน ราคา และหมวดหมู่ได้ทั้งหมด
- การจัดการหมวดหมู่: เพิ่ม/ลบได้ มีหมวดหมู่เริ่มต้น "อาหาร" และ "เครื่องดื่ม"
- การจัดการรายชื่อคนร่วมบิล: เพิ่ม/ลบ/แก้ไขชื่อ
- โหมดหารบิลแบบ "หารเท่ากันทุกคน" และ "หารตามรายการที่กิน" (พร้อมหารค่าบริการ/VAT ตามสัดส่วน)
- ค่าบริการ (service charge) และภาษีมูลค่าเพิ่ม (VAT) แบบกำหนดเปอร์เซ็นต์เองได้
- บันทึกสถานะบิลอัตโนมัติลง `localStorage` ของเบราว์เซอร์ (ไม่มีเซิร์ฟเวอร์ของตัวเอง)
- รองรับ PWA: `manifest.json` + `service-worker.js` เพื่อให้ "เพิ่มลงหน้าจอโฮม" และใช้งานออฟไลน์ได้
- ธีมภาพแบบ "กระดาษใบเสร็จ" (มุมฉีกซิกแซก, เส้นประรอยฉีก, ฟอนต์ Space Mono) พร้อมไอคอนแอป
- เอกสาร `SKILL.md` อธิบายโครงสร้างโปรเจกต์สำหรับแก้ไขในอนาคต

### หมายเหตุ
- ความแม่นยำของ OCR ขึ้นอยู่กับความชัดของรูปและรูปแบบใบเสร็จ ควรตรวจสอบรายการหลังสแกนเสมอ
