# CHANGELOG

รูปแบบ: [Keep a Changelog](https://keepachangelog.com/) แบบย่อ — ทุกครั้งที่แก้ไขโปรเจกต์ ให้เพิ่มหัวข้อใหม่ด้านบนสุดพร้อมวันที่ และระบุ `AI agent:` (หรือ `ผู้แก้ไข:` สำหรับการแก้ไขโดยมนุษย์) ในหัวข้อนั้นเสมอ

## [Unreleased] - 2026-08-24

**AI agent: Codex (`/root`)**

### เปลี่ยนแปลง (Changed)
- ปรับตำแหน่งแถบ navigation ของ flow ไปไว้ด้านล่างตามภาพอ้างอิง พร้อมพื้นที่เผื่อเนื้อหาและ safe area บนมือถือ และคงข้อความ label ของแท็บเดิมไว้ทั้งหมด
- เปลี่ยนไอคอน UI หลักให้ใช้ชุด Material Icons – Rounded ตาม MUI: navigation, การเริ่มต้นบิล, สถานะว่าง, การคำนวณ, การแชร์ และการนำทาง
- เพิ่มเวอร์ชัน service-worker cache (`v10` → `v12`) เพื่อให้ผู้ใช้ PWA ได้รับ CSS และ icon font ใหม่

## [Unreleased] - 2026-08-24

**AI agent: Claude (Opus 5)**

### แก้ไข (Fixed) — 4 หน้าที่แยกไม่ออกว่าเปลี่ยนหน้าแล้ว

- **หน้า "รายการอาหาร"/"ค่าใช้จ่ายเพิ่มเติม" และ "คนที่ร่วมกิน"/"ใครกินอะไร" ดูเหมือนหน้าเดียวกัน** — `PANEL_META` จับ 2 หน้าจอให้เป็น step เดียวกัน (`items`+`charges` = step 1, `people`+`assign` = step 2) และข้อความ eyebrow ยัง **hard-code ไว้ในทุก panel** จน `index.html` เขียนว่า "ขั้นตอนที่ 1 จาก 4" เหมือนกันเป๊ะทั้ง 2 หน้า ผลคือกด "ถัดไป" แล้วแถบขั้นตอนไม่ขยับ eyebrow ไม่เปลี่ยน มีแต่ `<h2>` ที่เปลี่ยน — รู้สึกเหมือนยังอยู่หน้าเดิม

### เปลี่ยนแปลง (Changed) — โครง 4 ขั้นตอน + แท็บย่อย และสลับลำดับให้เลือกวิธีหารก่อน

- **หนึ่งขั้นตอนมีได้หลายหน้าจอ และแสดงเป็น "แท็บย่อย" (`#substeps`)** ที่บอกตำแหน่งว่าเป็นหน้าที่เท่าไรของขั้นตอนนั้น กดสลับไปมาในขั้นตอนเดียวกันได้ — ใช้ภาษาภาพเดิมของ `.segmented` (ตัวเลือกโหมด VAT) ไม่ได้สร้าง component ใหม่
  - ขั้นตอน 1 **รายการ** → `รายการอาหาร` / `ค่าใช้จ่าย`
  - ขั้นตอน 3 **แบ่งบิล** → `วิธีหารบิล` / `ใครกินอะไร`
- **สลับลำดับ: เลือกวิธีหารบิลก่อน แล้วค่อยระบุ "ใครกินอะไร"** — เดิมต้องระบุคนกินก่อนเลือกวิธีหาร คนที่จะ "หารเท่ากัน" จึงเสียเวลาทำขั้นตอนที่ไม่มีผลต่อการคำนวณและไม่เข้าใจว่าทำไปทำไม ตอนนี้ **โหมดหารเท่ากันข้ามหน้า "ใครกินอะไร" ไปเลย** ทั้งปุ่มถัดไป ปุ่มย้อนกลับ และแท็บย่อย
  - เลือก "หารตามรายการที่กิน" แล้วแท็บย่อย "ใครกินอะไร" จะ **โผล่ขึ้นมาในที่** โดยไม่พาผู้ใช้ออกจากหน้า (สื่อว่าตัวเลือกนี้เพิ่มงานให้ 1 อย่าง) พร้อมข้อความบอกขั้นตอนถัดไปใต้ตัวเลือก
  - เพราะระดับบนยังคงที่ 4 ขั้นตอน จำนวนขั้นตอนจึงไม่แกว่งตามโหมด ความต่างถูกดูดเข้าไปเป็นแท็บย่อยของขั้นตอนที่ 3
  - **สลับโหมดไม่ล้างค่าที่ระบุไว้** — `item.consumerIds` ไม่ถูกแตะ สลับกลับมาได้ของเดิม
- **โครงขั้นตอนทั้งหมดย้ายไปอยู่ที่ `STEPS` ใน `js/ui.js` ที่เดียว** — แถบขั้นตอน, แท็บย่อย, ข้อความ "ขั้นตอนที่ N จาก 4", ป้ายปุ่มย้อนกลับ/ถัดไป ล้วน derive จากตารางนี้ (`flowFor(mode)` / `stepPanels()` / `nextTab()` / `previousTab()`) `.eyebrow` ใน HTML กลายเป็นแท็กเปล่าให้ JS เติม — ตัดต้นเหตุที่ทำให้ข้อความ 2 หน้าหลุดไม่ตรงกัน
- **ปุ่มย้อนกลับ/ถัดไปเปลี่ยนเป็นไอคอนลูกศรล้วน ไม่มีข้อความ** — วงกลม 48px (`.btn--icon`) ย้อนกลับชิดซ้าย ถัดไปชิดขวา ใช้ path ของ Material Icons ธีม Rounded จาก MUI (`ArrowBackIosRounded` / `ArrowForwardIosRounded`) **ฝัง SVG ไว้ในโค้ดตรง ๆ ไม่ได้โหลดจาก CDN** เพื่อให้แอปยังทำงานออฟไลน์ได้
  - ชื่อหน้าปลายทางย้ายไปอยู่ใน `aria-label` + `title` (เช่น `ถัดไป: ค่าใช้จ่ายเพิ่มเติม`) ปุ่มจึงยังมี accessible name และชี้ค้างบนเดสก์ท็อปยังเห็นว่าไปไหน
  - เปลี่ยนจาก `data-go-tab="<ชื่อหน้า>"` ที่ hard-code ปลายทางไว้ใน HTML เป็น `data-go="back|next"` ที่ resolve ตอนคลิก (จำเป็น เพราะปลายทางของขั้นตอนที่ 3 เปลี่ยนตามโหมด)
- **เพิ่มบรรทัดสรุปบริบท (`.panel-recap`)** บอกว่ากำลังทำงานบนข้อมูลชุดไหน เช่นหน้าค่าใช้จ่ายโชว์ `รายการอาหาร 5 อย่าง · ฿620.00`, หน้าคนกินโชว์ `ยอดบิลรวม ฿729.74 · 5 รายการ`
- `switchTab()` เพิ่ม guard: สั่งไปหน้าที่ไม่อยู่ใน flow ของโหมดปัจจุบันจะเด้งไปหน้าแรกของขั้นตอนนั้นแทน
- bump `CACHE_NAME` `v8` → `v9`

### เอกสาร (Documentation)
- `SKILL.md`: เขียนหัวข้อ "Flow ของหน้าจอ" ใหม่เป็นตาราง 4 ขั้นตอน + แผนผังทางแยกตามโหมด, บันทึกว่า flow เป็น dynamic (`flowFor`/`MODE_ONLY_PANELS`), ข้อห้าม hard-code ข้อความ/ปลายทางกลับไปใน HTML และข้อควรระวังว่า `renderAssignList()` ต้องรันเสมอแม้หน้า `assign` ไม่อยู่ใน flow (เทสต์ integration พึ่งอยู่)


### เปลี่ยนแปลง (Changed) — เปลี่ยนชุดไอคอนเป็น Material Symbols Rounded (FILL 0) แล้วปรับแถบเมนูล่างให้ใกล้เคียง reference

- ผู้ใช้ขอให้ใช้ไอคอนจากคอลเล็กชัน "Basic Rounded Lineal" ของ Magnific/Freepik — **ตรวจแล้วเป็นคลังไอคอนมีลิขสิทธิ์ (ต้องซื้อสิทธิ์หรือใส่เครดิต) ดาวน์โหลดไฟล์ SVG มาฝังโค้ดตรง ๆ ไม่ได้** จึงเปลี่ยนไปใช้ **Material Symbols Rounded พร้อม `font-variation-settings: 'FILL' 0`** แทน (เส้นขอบมน ปลายมน สไตล์ใกล้เคียงกัน เป็นโอเพนซอร์สใช้ได้ฟรี) ชื่อไอคอนใน HTML ใช้ชุดเดิมของ Material Icons ได้ทั้งหมดเพราะ Material Symbols รองรับชื่อชุดเดิม — เปลี่ยนแค่ font + `font-variation-settings` ใน `.material-icons-round` (`css/style.css`)
- ปุ่มลบรายการ/ลบคน (`.row-del`) และปุ่มปรับจำนวน (`.qty-btn`) เปลี่ยนจากอักขระ `×`/`−`/`+` เป็นไอคอนจริง (`delete_outline`, `close`, `remove`, `add`) ให้สื่อความหมายชัดขึ้น
- **ปรับ badge ไอคอนบนแถบเมนูล่างจากวงกลม (22×22px) เป็นทรงแคปซูล/pill (40×26px)** ให้ใกล้เคียงกับภาพ reference ที่ผู้ใช้ส่งมา และปรับพื้นหลัง badge ตอน inactive ให้อ่อนลง (`--surface-soft` แทน `#e6ebe9`) — เปลี่ยน glyph ของแท็บ "คนกิน" จาก `group` (ไอคอนคน 2 คน) เป็น `person` (ไอคอนคนเดียว) ให้ใกล้เคียง silhouette ในภาพมากขึ้น
  - **ยังไม่ใช่การจับคู่แบบเป๊ะ 100% กับ reference** เพราะ silhouette จริงของแต่ละไอคอนมาจากชุดที่มีลิขสิทธิ์ ปรับได้แค่รูปทรง/สี/ระยะห่างให้ใกล้เคียงที่สุดเท่าที่ใช้ไอคอนฟรีได้ ตัว glyph จะไม่มีทางเหมือนเส้นต่อเส้น
- bump `CACHE_NAME` `v12` → `v14`
---

## [Unreleased] - 2026-08-21

**AI agent: Claude (Opus 5)**

### เปลี่ยนแปลง (Changed) — ปรับ UX/UI ทั้งเว็บตาม Blueprint และเปลี่ยนเป็นเว็บมือถือคอลัมน์เดียว

- **เอา sidebar ด้านซ้ายออกทั้งหมด เปลี่ยนเป็น layout แบบเว็บมือถือคอลัมน์เดียว** (`.app` กว้างสุด 520px จัดกลางจอ) — บนจอกว้างยังเป็นคอลัมน์เดียวเหมือนเดิม แค่ยกเป็นการ์ดลอยมุมโค้ง ไม่ใช่ desktop layout ที่มีเมนูข้าง
- **แยก flow ออกเป็น 8 หน้าจอตาม Blueprint** (เดิมมี 4 แท็บที่ยัดหลายเรื่องรวมกัน): `landing` → `scan` → `items` → `charges` → `people` → `assign` → `method` → `split` โดยแยก "ค่าใช้จ่ายเพิ่มเติม (VAT/ค่าบริการ)" ออกจากหน้ารายการอาหาร และแยก "ใครกินอะไร" ออกจากหน้ารายชื่อคน
  - เพิ่มหน้า **Landing** (hero + "เริ่มใช้งาน") และปุ่ม **"ทำบิลเดิมต่อ"** ที่โชว์เฉพาะเมื่อมีบิลค้างใน localStorage
  - เพิ่ม **app bar** พร้อมปุ่มย้อนกลับที่ไล่ตาม flow อัตโนมัติ และปุ่ม `↺` เริ่มบิลใหม่
  - ลำดับหน้าจอ/ชื่อหัวข้อรวมไว้ที่ `FLOW` + `PANEL_META` ใน `js/ui.js` ที่เดียว
- **หน้า `scan` เปลี่ยนเป็น 3 ตัวเลือกเท่ากันตาม Blueprint**: สแกนใบเสร็จ / เพิ่มรายการเอง / นำเข้ารูปภาพ
- **เปลี่ยนธีมเป็น "Calm Utility 2026" (Warm Gray + Muted Teal) และเขียน `css/style.css` ใหม่ทั้งไฟล์** — เดิมเป็นธีมเข้ม terracotta แล้วมีบล็อก override ธีมใหม่ทับท้ายไฟล์ ทำให้มี token ชุดเดียวที่ถูกประกาศซ้ำ 2 รอบและอ่าน/แก้ต่อยากมาก ตอนนี้เหลือ token ชุดเดียว mobile-first
- **สีประจำคนรองรับเกิน 5 คนแล้ว** — เดิมใช้ CSS `:nth-child` จึงมีสีแค่ 5 คนแรก (คนที่ 6 ขึ้นไปเป็นสีเทาหมด) ตอนนี้ `ui.js` คำนวณ `data-tone="0..7"` จากลำดับใน `bill.people` แล้ววนซ้ำ 8 โทน และใช้โทนเดียวกันทุกหน้าจอ
- **ข้อความ "ยอดรวมตรงกับใบเสร็จแล้ว" เปลี่ยนเป็นการตรวจจริง** — เดิมเป็นข้อความตายตัวที่แสดงเสมอไม่ว่ายอดจะตรงหรือไม่ ตอนนี้ `renderSummaryPanel()` เทียบ `Σ ยอดต่อคน` กับ `ยอดรวมทั้งบิล` ระดับสตางค์ ถ้าไม่ตรงจะไม่แสดงเลย
- ปุ่มแชร์/คัดลอกย้ายไปอยู่ใน **bottom sheet "แชร์ผลลัพธ์"** พร้อมแชร์ผ่าน LINE และดาวน์โหลดสรุปเป็นรูป PNG (วาดด้วย canvas API เอง ไม่พึ่ง library ภายนอก เพื่อให้ยังทำงานออฟไลน์ได้) — ข้อความที่แชร์คำนวณจาก state จริงแล้ว ไม่ได้อ่านค่าจาก DOM เหมือนเดิม
- เพิ่ม state ที่ขาดไปให้ครบตาม Blueprint: empty state ที่มีปุ่มลงมือต่อได้ในตัว, แถบเตือนจำนวนรายการที่ยังไม่ระบุคนกิน (หน้า `assign`), และสถานะ loading/สำเร็จ/ผิดพลาดของการอ่านใบเสร็จ (`ui.setScanStatus()`)
- เพิ่ม `.claude/launch.json` ให้เปิดตัวอย่างเว็บผ่าน `preview-server.cjs` ได้ทันที

### เพิ่ม (Added) — หน้ารายละเอียดรายการอาหารของแต่ละคน

- **แตะการ์ดของคนใดคนหนึ่งในหน้าสรุปผล จะเปิด bottom sheet แสดง list รายการอาหารที่คนนั้นหาร** (`ui.openPersonSheet()` → `#personSheetOverlay`) แต่ละบรรทัดบอกชื่อรายการ, ราคาเต็มของรายการ, จำนวนคนที่หารร่วมกัน และส่วนแบ่งเป็นเงินของคนนั้น ปิดได้ 3 ทาง (ปุ่ม × / Escape / แตะพื้นหลัง)
  - โหมด **หารตามรายการที่กิน**: ตัวเลขกระทบยอดกันครบ — `ค่าอาหาร + ส่วนแบ่งค่าบริการ/ภาษี = ยอดที่ต้องจ่าย` และผลรวมส่วนแบ่งทุกรายการเท่ากับค่าอาหารของคนนั้นพอดี
  - โหมด **หารเท่ากัน**: แสดง `ยอดรวมทั้งบิล ÷ จำนวนคน` พร้อมหมายเหตุกำกับว่ารายการที่แสดงเป็นข้อมูลอ้างอิง ไม่ได้เป็นฐานคำนวณ (กันเข้าใจผิดว่ายอดมาจากรายการที่กิน)
- การ์ดสรุปยังโชว์ chip รายการอาหารเหมือนเดิม แต่จำกัดที่ 10 รายการแล้วยุบส่วนเกินเป็น `+N` (เดิมยาวเท่าไรก็แสดงหมด ทำให้การ์ดสูงมากเมื่อมีรายการเยอะ) — รายละเอียดทั้งหมดดูได้ใน sheet
- `getConsumptionSummary()` ใน `js/splitter.js` คืน `lineTotal`/`sharedWith`/`share`/`isShared` เพิ่มในแต่ละรายการ (ของเดิม `name`/`qty` ยังอยู่ครบ) และกรอง `consumerIds` ที่อ้างถึงคนที่ถูกลบไปแล้วออก
- เพิ่ม `countUnassignedItems()` ใน `js/splitter.js` สำหรับแถบเตือนในหน้า `assign`

### แก้ไข (Fixed)

- **แถบบน (app bar + ขั้นตอน) เลื่อนหายตอน scroll บนจอกว้าง** — `.app` มี `overflow: hidden` ไว้ตัดมุมโค้ง ซึ่งสร้าง scroll container ที่ไม่เลื่อน ทำให้ `position: sticky` ใช้งานไม่ได้ ตอนนี้ให้ `.app` เป็น scroll container ของตัวเอง (`height` + `overflow-y: auto`) และ `ui.switchTab()` รีเซ็ต `#app.scrollTop` ด้วย
- **แถบขั้นตอนเลื่อนไปซ้อนใต้ app bar ตอน scroll** — เดิม sticky แยกกันแล้ว hard-code `top: 61px` ซึ่งไม่ตรงกับความสูงจริง (68px และเปลี่ยนได้ตาม `env(safe-area-inset-top)`) ตอนนี้ครอบทั้งสองไว้ใน `.topbar` ที่ sticky เป็นก้อนเดียว

### เอกสาร (Documentation)
- `SKILL.md`: เพิ่มหัวข้อ "Flow ของหน้าจอ" (ตารางหน้าจอทั้ง 8 + ขั้นตอน) และ "จุดที่ควรรู้เรื่อง layout" (กับดัก `overflow: hidden` vs `position: sticky`, `.topbar`, ระบบสีต่อคน), อัปเดตหัวข้อโครงสร้างไฟล์/ธีมสี/การเริ่มบิล และบันทึกว่าเทสต์ต้องใช้ **Node.js 20+** (jsdom ปัจจุบันพังบน Node 18 ด้วย `ERR_REQUIRE_ESM`)

---

**AI agent: Codex (`/root`)**

### เปลี่ยนแปลง (Changed)
- เริ่มปรับโครงสร้างและหน้าตา Receipt Splitter ตาม UX/UI Blueprint ใหม่: โทน Warm Gray + Muted Teal, navigation แบบ desktop sidebar, ขั้นตอนการทำงานที่มองเห็นได้ชัด และหน้าสรุปแบบ person card พร้อมรายการอาหารของแต่ละคน
- เพิ่ม summary panel ในหน้าสรุปยอด พร้อม action สำหรับแชร์หรือคัดลอกผลลัพธ์, ปรับ responsive layout ให้ panel ย้ายลงล่างบนมือถือ และเพิ่มปุ่มนำทางย้อนกลับ/ถัดไปใน flow
- เพิ่ม `preview-server.cjs` สำหรับเปิดตัวอย่างเว็บแบบ local โดยส่ง MIME type ที่ถูกต้องให้ CSS และ ES modules

### เอกสาร (Documentation)
- กำหนดกติกาให้ `CHANGELOG.md` ระบุ AI agent หรือผู้แก้ไขทุกครั้งที่มีการเปลี่ยนโค้ด

## [1.8.0] - 2026-08-17

### แก้ไข (Fixed) — จูนการอ่านใบเสร็จ (OCR) ให้แม่นยำขึ้น

- **เพิ่มขั้นประมวลผลรูปก่อนส่งเข้า Tesseract**: `recognizeReceiptText()` ใน `js/ocr.js` ตอนนี้
  ขยายรูปที่เล็กเกินไป แปลง grayscale แล้ว binarize (ดำ-ขาวล้วน) ด้วย threshold ที่คำนวณอัตโนมัติ
  จาก Otsu's method ก่อนส่งเข้า OCR — ช่วยแก้ปัญหารูปถ่ายใบเสร็จจากมือถือที่คอนทราสต์ต่ำ/กระดาษ
  ซีดจากเครื่องพิมพ์ความร้อน ซึ่งเป็นสาเหตุหลักที่ Tesseract อ่านภาษาไทยผิดเพี้ยน ถ้าประมวลผลรูป
  ไม่สำเร็จ (เบราว์เซอร์เก่า) จะ fallback ไปอ่านรูปต้นฉบับตรง ๆ ไม่บล็อกฟีเจอร์
- **ย้ายจาก `Tesseract.recognize()` (deprecated) ไปใช้ `createWorker()` เพื่อปรับ PSM/OEM ได้**: ตั้ง
  `tessedit_pageseg_mode = SINGLE_BLOCK` (เหมาะกับใบเสร็จที่เป็นข้อความไหลต่อกันบล็อกเดียว ลดปัญหา
  Tesseract เดา layout ผิดตอนรูปเอียง/ไม่ชัดเทียบกับโหมด AUTO เดิม) และ `OEM = LSTM_ONLY`
- **แก้ปัญหาบรรทัดที่ไม่ใช่รายการอาหารหลุดเข้าไปเป็น "รายการอาหาร" ปลอม เมื่อ OCR อ่านภาษาไทยเพี้ยน**:
  พบจากการ investigate ใบเสร็จจริงว่า Tesseract มักแทรกช่องว่างระหว่างตัวอักษรไทยแทบทุกตัว (เช่น
  "คิวที่" กลายเป็น "ค ิ ว ท ี ่") ทำให้ `SKIP_WORDS`/`FOOTER_PATTERNS` ที่ match แบบ substring/regex
  ตรง ๆ ไม่เจอคำเลย เพิ่มฟังก์ชัน `collapseThaiCharSpacing()` ใน `js/ocr.js` ให้ `isSkipLine`/
  `matchFooterPattern` เรียกก่อนเทียบเสมอ (จำกัดแค่ตัวอักษรไทยคั่นด้วยวรรค ไม่แตะวรรคระหว่างคำ
  อังกฤษ/ตัวเลข กัน `\b` word boundary ของ pattern อื่นพัง) พร้อมเพิ่มคำที่ยังขาดอยู่:
  `'เงินสด'`/`'เงินทอน'`/`'cash'`/`'change'` ใน `SKIP_WORDS` และ `'รวมมูลค่า'` ใน `FOOTER_PATTERNS`
  (คำที่ POS บางระบบใช้แทน "ยอดรวม")
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v6` → `v7`)

### เพิ่ม (Added)

- เทสต์ใหม่ใน `tests/ocr.test.js`: `computeOtsuThreshold()` (คำนวณ threshold แยกกลุ่มมืด/สว่าง,
  ภาพสีเดียว, histogram ว่าง) และเคสใบเสร็จจริงที่ตัวอักษรไทยเว้นวรรค (คิว/เงินทอน ต้องไม่กลายเป็น
  รายการอาหาร, "รวมมูลค่า" ต้องถูกจับเป็น total)

## [1.7.0] - 2026-08-14

### แก้ไข (Fixed)
- **ปัดเศษสตางค์ตอนหารบิล**: `splitEqual`/`splitItemized` ใน `js/splitter.js` เดิมคำนวณเป็น float ล้วน ไม่ปัดเศษเลย ทำให้บางกรณี (เช่น หาร ฿100 ให้ 3 คน) ยอดที่แสดงต่อคนรวมกันได้ `99.99` ไม่ตรงกับยอดสุทธิทั้งบิลที่แสดง `100.00` ตอนนี้ปัดยอดแต่ละคนเป็นสตางค์แล้วเกลี่ยเศษที่เหลือให้คนที่มีเศษทศนิยมมากที่สุดก่อน (largest-remainder method) ทำให้ผลรวมของยอดที่แสดงตรงกับยอดบิลเป๊ะเสมอ
- **ปุ่ม "✅ ทุกคน" ในหน้า "ใครกินอะไรบ้าง" กดซ้ำได้แล้ว (toggle)**: เดิมกดปุ่มนี้แล้วเลือกทุกคนอย่างเดียว กดซ้ำก็ยังเป็น "ทุกคน" เหมือนเดิม ต้องไปแตะถอดทีละคนเอง ตอนนี้ถ้ากดตอนที่ทุกคนถูกเลือกอยู่แล้ว จะยกเลิกเลือกทุกคนพร้อมกัน (กลับไปเป็นสถานะ "ยังไม่ระบุ" ซึ่งระบบจะหารรายการนั้นให้ทุกคนเท่า ๆ กันโดยอัตโนมัติเหมือนเดิม)

### เปลี่ยนแปลง (Changed) — เก็บกวาดโค้ดตาย
- **ลบฟีเจอร์ "หมวดหมู่รายการ" ที่ตายไปแล้วออกทั้งหมด**: `categories`/`categoryId` ถูกถอด UI ออกไปตั้งแต่เวอร์ชันก่อน ๆ (การ์ดรายการไม่มีตัวเลือกหมวดหมู่มานานแล้ว) แต่โค้ดยัง seed/persist `categories` ค้างอยู่ใน `js/storage.js` และยังตั้ง `categoryId: null` ตายตัวใน `js/app.js` — ตอนนี้ลบออกให้สะอาด พร้อมแก้ `README.md`/`SKILL.md` ที่ยังบรรยายว่าเลือกหมวดหมู่ได้ให้ตรงกับโค้ดจริง ข้อมูลบิลเก่าที่มี `categories`/`categoryId` ค้างอยู่ใน localStorage ยังโหลดได้ปกติ (แค่เพิกเฉยฟิลด์นั้น)
- ลบ parameter `bill` ที่ไม่ได้ใช้แล้วออกจาก `ui.openItemModal()` (ตกค้างจากตอนที่ยังมี dropdown เลือกหมวดหมู่ใน popup)
- ลบ CSS ที่ไม่ถูกอ้างถึงที่ไหนเลย (`.empty-hint__sub`, `.empty-hint--illustrated`)
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v5` → `v6`)

## [1.6.0] - 2026-08-09

### เปลี่ยนแปลง (Changed)
- **เอาเลขลำดับ (①②③④) ออกจากไอคอนแท็บ** เหลือแค่ไอคอน + ชื่อแท็บ
- **เพิ่มรายการอาหารผ่าน popup**: กด "+ เพิ่มรายการ" (ทั้งจากแท็บรายการ และปุ่ม "เริ่มกรอกรายการเอง") จะเปิด popup ให้กรอกชื่อ/จำนวน/ราคา/หมวดหมู่ก่อนเพิ่ม แทนการสร้างการ์ดเปล่าให้กรอกทีหลัง — ปิด popup ได้ 3 ทาง: กด "ยกเลิก", กด Escape, หรือคลิกพื้นหลังนอกกล่อง (ทุกทางไม่เพิ่มรายการ)
- **"เริ่มบิลใหม่" เก็บรายชื่อคนไว้เหมือนเดิม**: ล้างรายการอาหาร/หมวดหมู่/การตั้งค่า VAT-ค่าบริการทั้งหมด แต่ไม่ลบรายชื่อคนที่เคยเพิ่มไว้ (ไม่ต้องเพิ่มชื่อซ้ำทุกครั้งที่เริ่มบิลใหม่กับกลุ่มเดิม)

### เพิ่ม (Added)
- **เมนูหารบิลแสดงสรุปรายการที่แต่ละคนกินด้วย**: การ์ดผลลัพธ์ของแต่ละคน (ทั้งโหมดหารเท่ากันและหารตามรายการ) มีรายชื่อเมนูที่คนนั้นกินแสดงเป็น chip เล็ก ๆ ต่อท้าย
- **ปุ่ม "✅ ทุกคน" ในหน้า "ใครกินอะไรบ้าง"**: กดครั้งเดียวเลือกทุกคนในรายชื่อให้เป็นคนกินรายการนั้นทันที (แทนการต้องกดเลือกทีละคน) ยังแตะเอาคนใดคนหนึ่งออกได้ตามปกติหลังจากนั้น
- เทสต์ใหม่ 21 ตัว ครอบคลุมพฤติกรรม popup, ปุ่มเลือกทั้งหมด, สรุปรายการที่กิน, และการเก็บรายชื่อคนไว้ตอนเริ่มบิลใหม่ (`getConsumptionSummary` ใน `splitter.js`)
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v3` → `v4`)

## [1.5.0] - 2026-08-09

### เปลี่ยนแปลง (Changed) — ปรับปรุงการอ่านใบเสร็จใน `js/ocr.js` ครั้งใหญ่
- **แบ่งส่วนใบเสร็จด้วยเส้นคั่น**: รองรับโครงสร้างใบเสร็จทั่วไปที่แบ่งเป็น 3 ส่วนด้วยเส้นคั่น (`----`/`====`/`....`) คือ หัวร้าน (ชื่อ/วันเวลา/โต๊ะ) → รายการอาหาร → สรุปยอด ระบบจะตัดเอาแค่ "ส่วนรายการอาหาร" มาพาร์สเป็นรายการ ทำให้ไม่หลุดเอาข้อมูลหัวร้าน/ท้ายบิลมาเป็นรายการอาหารผิด ๆ แม้บรรทัดนั้นจะไม่มีคำอยู่ในคำสำคัญที่กรองไว้ก็ตาม ถ้าใบเสร็จไม่มีเส้นคั่นเลย ระบบจะกรองด้วยคำสำคัญเหมือนเดิม (ใกล้เคียงที่สุดที่ทำได้)
- **ยึดหัวตารางเมื่อมี**: ถ้าอ่านเจอบรรทัดหัวตารางของลิสรายการ (เช่น "รายการ  จำนวน  ราคา") จะยึดตำแหน่งคอลัมน์ตามหัวตารางนั้นในการแยกชื่อ/จำนวน/ราคาของแต่ละแถว แม่นยำกว่าการเดาแบบเดิม รองรับทั้งกรณีคอลัมน์เป็น "ราคาต่อหน่วย" และกรณีเป็น "ยอดรวมต่อแถว" (จะหารด้วยจำนวนให้กลับมาเป็นราคาต่อหน่วยให้อัตโนมัติ) ถ้าไม่เจอหัวตาราง จะ fallback ไปใช้วิธีเดาแบบเดิม
- **ดึงยอดสรุปท้ายบิล**: อ่าน subtotal / ส่วนลด / ค่าบริการ / VAT / ยอดรวม จากใบเสร็จ (ถ้ามี) เก็บไว้ใน `footerTotals`
- **เทียบยอด (reconciliation)**: เทียบยอดรวมที่พาร์สรายการอาหารได้ กับ subtotal/ยอดรวมที่พิมพ์ไว้ในใบเสร็จ ถ้าไม่ตรงกัน (คลาดเคลื่อนเกิน 1 บาท) จะแสดง toast เตือนให้ผู้ใช้ตรวจสอบรายการอีกครั้งหลังแปลงจาก OCR (ยังคงเพิ่มรายการที่อ่านได้ให้เสมอ ไม่บล็อกการใช้งาน แค่เตือน)
- **Breaking change (ภายในโค้ดเท่านั้น ไม่กระทบผู้ใช้)**: `parseReceiptLines()` เปลี่ยนจากคืนค่า array ตรง ๆ เป็นคืนค่า `{ items, footerTotals, reconciliation }` — อัปเดต `js/app.js` และชุดเทสต์ให้ตรงกับ signature ใหม่แล้ว

### เพิ่ม (Added)
- เทสต์ใหม่ 13 ตัวใน `tests/ocr.test.js` ครอบคลุมการแบ่งส่วนด้วยเส้นคั่น, การยึดหัวตาราง, การดึงยอดสรุป, และการเทียบยอด
- เทสต์ integration ใหม่ยืนยันว่า toast เตือนยอดไม่ตรงกันแสดงผลจริงผ่าน UI (ไม่ใช่แค่ยูนิตเทสต์ฟังก์ชันเปล่า ๆ)
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v2` → `v3`)

## [1.4.0] - 2026-08-09

### เพิ่ม (Added)
- **เริ่มบิลได้โดยไม่ต้องสแกน**: แท็บแรก (เดิมชื่อ "สแกน" เปลี่ยนเป็น "เริ่มต้น") ตอนนี้แสดงตัวเลือกชัดเจน 2 ทาง — "สแกนใบเสร็จ" หรือ "กรอกเอง" — กดปุ่ม "เริ่มกรอกรายการเอง" จะพาไปแท็บรายการพร้อมช่องกรอกเปล่าให้ทันที ไม่ต้องมีใบเสร็จเลยก็ใช้แอปได้
- คำแนะนำใน `README.md` เรื่องวิธีเปิดไฟล์ `index.html` ให้ถูกต้อง (ต้องแตกไฟล์ zip ก่อน ห้ามเปิดจากข้างในหน้าต่าง ZIP ตรง ๆ) พร้อมอธิบายเหตุผลทางเทคนิคว่าทำไม PWA ถึงใช้งานไม่ได้ถ้าเปิดผ่าน `file://`
- คำแนะนำการฝากไฟล์ขึ้นเว็บแบบละเอียดใน `README.md` เปรียบเทียบ **GitHub Pages** (แนะนำ) กับ **Firebase Hosting** (ตัวเลือกรอง) พร้อมคำสั่ง git ที่พร้อมรันได้ทันที

### แก้ไข (Fixed)
- เพิ่มเลขเวอร์ชัน cache ใน `service-worker.js` (`v1` → `v2`) เพื่อให้ผู้ใช้ที่ติดตั้ง PWA ไว้แล้วได้รับโค้ดเวอร์ชันล่าสุด ไม่ติดแคชเก่าค้าง
- ลบ element `#scanIdleHint` ที่ไม่จำเป็นแล้ว (ถูกแทนที่ด้วย UI ตัวเลือกเริ่มต้นที่ชัดเจนกว่า)

### หมายเหตุสำคัญ
- ปัญหา "เว็บไม่มีสไตล์เลย เป็นตัวหนังสือดำล้วน" ที่พบตอนเปิดไฟล์จากข้างใน ZIP **ไม่ใช่บั๊กของโค้ด** แต่เกิดจาก Windows แตกไฟล์ `index.html`ออกมาไฟล์เดียวโดยไม่มีโฟลเดอร์ `css/`/`js/` ติดมาด้วย วิธีแก้คือแตกไฟล์ zip ให้ครบก่อนเปิด (ดู README) หรือฝากขึ้นเว็บโฮสติ้งแล้วเปิดผ่านลิงก์แทน

## [1.3.0] - 2026-08-09

### เพิ่ม (Added)
- ชุดเทสต์ครบทั้งโปรเจกต์ในโฟลเดอร์ `tests/` (35 เทสต์ / 10 กลุ่ม) รันด้วย `npm install && npm test` โดยใช้ Node.js built-in test runner (`node:test`) ไม่ต้องพึ่ง framework ทดสอบภายนอก:
  - `splitter.test.js` — ยูนิตเทสต์การคำนวณ (VAT 2 โหมด, ค่าบริการเปิด/ปิด, edge case ต่าง ๆ)
  - `ocr.test.js` — ยูนิตเทสต์ heuristic แปลง OCR เป็นรายการ
  - `storage.test.js` — ยูนิตเทสต์ localStorage และการ migrate ข้อมูลเก่า
  - `integration.test.js` — เทสต์ end-to-end ที่รัน **โค้ดจริงของแอป** บน DOM จำลอง (jsdom) จากไฟล์ `index.html` ตัวจริง จำลองผู้ใช้ทำงานครบวงจร (สแกน → รายการ → คนกิน → VAT/ค่าบริการ → หารบิล → เริ่มบิลใหม่) พิสูจน์ว่าใช้งานได้จริง ไม่ใช่แค่ทดสอบฟังก์ชันแยกส่วน
- `package.json` (สำหรับนักพัฒนาเท่านั้น ไม่จำเป็นสำหรับผู้ใช้ทั่วไป) พร้อม devDependency `jsdom` สำหรับรันเทสต์ end-to-end

### แก้ไข (Fixed) — บั๊กจริงที่เทสต์ชุดนี้ตรวจพบ
- **`js/ocr.js`**: regex จับราคาเดิม (`[0-9]{1,3}...`) จับตัวเลขได้แค่ 3 หลักท้ายเมื่อราคาไม่มีคอมมาแบ่งหลักและยาวเกิน 3 หลัก (เช่น "999999" จะถูกจับเป็น "999") ทำให้ตัวกรอง "ราคาไม่สมเหตุสมผล (>100,000)" หลุดไป แก้เป็น `[0-9]+...` เพื่อจับตัวเลขทั้งจำนวนก่อนกรอง
- **`tests/integration.test.js`**: Node.js เวอร์ชันใหม่มี `global.navigator` เป็น getter-only (สำหรับ fetch API) การ assign ตรง ๆ ทำให้ throw ต้องใช้ `Object.defineProperty` แทน

## [1.2.0] - 2026-08-09

### เพิ่ม (Added)
- ตัวเลือก **VAT**: เปิด/ปิดได้ (สวิตช์), เมื่อเปิดแล้วเลือกได้ว่า "ราคายังไม่รวม VAT" (ระบบบวก VAT เพิ่มให้) หรือ "ราคารวม VAT แล้ว" (ระบบแยกยอด VAT ที่แอบซ่อนอยู่ในราคาให้ดู โดยไม่บวกซ้ำ) พร้อมกำหนดอัตราภาษีเองได้ (ค่าเริ่มต้น 7%)
- ตัวเลือก **ค่าบริการ (Service charge)**: เปิด/ปิดได้ (สวิตช์), เมื่อเปิดแล้วกำหนดอัตราค่าบริการเองได้ (ค่าเริ่มต้น 10%)
- ป้ายกำกับในกล่องสรุปยอด (totals box) ปรับข้อความอัตโนมัติตามโหมด VAT ที่เลือก เช่น "รวมรายการ (รวม VAT แล้ว)" หรือ "VAT ที่รวมอยู่ในราคาแล้ว"
- ระบบ migrate ข้อมูลบิลเก่า (ที่บันทึกไว้ก่อนมีตัวเลือกนี้) ให้เข้ากับโครงสร้างใหม่โดยอัตโนมัติ ไม่ทำให้ข้อมูลเดิมพัง

### เปลี่ยนแปลง (Changed)
- `calcBillTotals()` ใน `js/splitter.js` คำนวณแยกกรณี "ราคารวม VAT แล้ว" โดยแยกฐานราคาก่อนภาษีออกมาก่อนคิดค่าบริการ (แทนที่จะบวก VAT ทับซ้ำ)
- `splitItemized()` เปลี่ยนมาคำนวณส่วนค่าบริการ/ภาษีที่หารตามสัดส่วนจาก `grandTotal - subtotal` แทนการบวก serviceAmount+vatAmount ตรง ๆ เพื่อให้ถูกต้องไม่ว่าจะเลือกโหมด VAT แบบไหนก็ตาม

## [1.1.0] - 2026-08-09

### เปลี่ยนแปลง (Changed)
- รีธีมทั้งเว็บเป็นสีครีม-ส้ม-ขาว (cream/orange/white) แทนธีมกระดาษใบเสร็จสีเข้มแบบเดิม เปลี่ยนฟอนต์หัวข้อเป็น Baloo 2 (ตัวกลม อ่านง่าย เป็นกันเอง)
- ปรับแท็บเมนูให้มีไอคอนและเลขขั้นตอน (1-4) กำกับ เพื่อให้เห็นลำดับการใช้งานชัดเจนขึ้น
- เปลี่ยนตารางรายการอาหาร (ที่แน่นและกดยากบนมือถือ) เป็นการ์ดรายการแบบ list พร้อมปุ่ม +/- ปรับจำนวนแบบแตะง่าย ไม่ต้องพิมพ์เลข
- เพิ่มวงกลมอักษรย่อ (avatar) ให้แต่ละคนในหน้ารายชื่อ หน้าระบุคนกิน และหน้าสรุปผลหารบิล ช่วยให้แยกคนได้ง่ายขึ้นด้วยสายตา
- ปรับปุ่มทั้งหมดเป็นทรงพิลล์ (rounded) และขยายพื้นที่กดให้ใหญ่ขึ้นสำหรับใช้งานบนมือถือ
- เพิ่ม empty state แบบมีอิโมจิประกอบและคำแนะนำที่ชัดเจนขึ้นในทุกแท็บที่ยังไม่มีข้อมูล

### แก้ไข (Fixed)
- อัปเดตตัวเลือกใน `js/app.js`/`js/ui.js` ให้ตรงกับโครงสร้าง DOM ใหม่ (การ์ดรายการแทนตาราง) พร้อมทดสอบว่าไม่มี id/selector หลุดอ้างอิงผิด

## [1.0.0] - 2026-08-09

### เพิ่ม (Added)
- โครงสร้างเว็บแอปเริ่มต้นทั้งหมด: สแกนใบเสร็จ, จัดการรายการ, จัดการหมวดหมู่, จัดการรายชื่อคน, หารบิล
- ระบบสแกนใบเสร็จด้วย Tesseract.js (OCR ทำงานในเบราว์เซอร์ ไม่ส่งรูปขึ้นเซิร์ฟเวอร์) พร้อม heuristic แปลงข้อความเป็นรายการอาหาร (`js/ocr.js`)
- การจัดการรายการอาหาร: เพิ่ม/ลบ/แก้ไขชื่อ จำนวน ราคา และหมวดหมู่ได้ทั้งหมด
- การจัดการหมวดหมู่: เพิ่ม/ลบได้ มีหมวดหมู่เริ่มต้น "อาหาร" และ "เครื่องดื่ม"
- การจัดการรายชื่อคนร่วมบิล: เพิ่ม/ลบ/แก้ไขชื่อ
- โหมดหารบิลแบบ "หารเท่ากันทุกคน" และ "หารตามรายการที่กิน" (พร้อมหารค่าบริการ/VAT ตามสัดส่วน)
- ค่าบริการ (service charge) และภาษีมูลค่าเพิ่ม (VAT) แบบกำหนดเปอร์เซ็นต์เองได้
- บันทึกสถานะบิลอัตโนมัติลง `localStorage` ของเบราว์เซอร์ (ไม่มีเซิร์ฟเวอร์ของตัวเอง)
- รองรับ PWA: `manifest.json` + `service-worker.js` เพื่อให้ "เพิ่มลงหน้าจอโฮม" และใช้งานออฟไลน์ได้
- ธีมภาพแบบ "กระดาษใบเสร็จ" (มุมฉีกซิกแซก, เส้นประรอยฉีก, ฟอนต์ Space Mono) พร้อมไอคอนแอป
- เอกสาร `SKILL.md` อธิบายโครงสร้างโปรเจกต์สำหรับแก้ไขในอนาคต

### หมายเหตุ
- ความแม่นยำของ OCR ขึ้นอยู่กับความชัดของรูปและรูปแบบใบเสร็จ ควรตรวจสอบรายการหลังสแกนเสมอ
