# SKILL.md — หารบิล (Receipt Splitter)

เอกสารนี้อธิบายโครงสร้างและแนวคิดของโปรเจกต์ สำหรับใช้อ้างอิงเวลากลับมาแก้ไข/ต่อยอดโค้ดในอนาคต (ทั้งสำหรับคนและสำหรับ Claude หรือ AI ตัวอื่นที่ช่วยแก้โค้ด)

## ภาพรวม

เว็บแอปสำหรับสแกนใบเสร็จร้านอาหารด้วย OCR แล้วหารค่าใช้จ่ายกับเพื่อน ทำงานฝั่งไคลเอนต์ (client-side) ทั้งหมด **ไม่มีเว็บเซิร์ฟเวอร์ของตัวเอง** — เปิดจากไฟล์ `index.html` ได้โดยตรง หรือฝากไว้บน static hosting ใดก็ได้ (GitHub Pages, Netlify, Cloudflare Pages ฯลฯ) และรองรับการ "เพิ่มลงหน้าจอโฮม" บนมือถือผ่าน PWA manifest + service worker

## หลักการออกแบบ

1. **ไม่มี build step** — ใช้ vanilla JavaScript (ES Modules) ล้วน ๆ ไม่มี bundler/framework ไม่ต้อง `npm install` ก่อนใช้งาน เปิดไฟล์แล้วรันได้ทันที
2. **แยกหน้าที่ชัดเจนต่อไฟล์ (single responsibility)** — ดูหัวข้อ "โครงสร้างไฟล์" ด้านล่าง
3. **State เดียว ความจริงเดียว (single source of truth)** — ตัวแปร `bill` ใน `js/app.js` คือสถานะทั้งหมดของบิลปัจจุบัน ทุกการเปลี่ยนแปลงต้องผ่าน handler ใน `app.js` แล้วเรียก `persist()` + เรียก `ui.render*()` ที่เกี่ยวข้องเสมอ
4. **ui.js ไม่มี business logic** — มีหน้าที่ "อ่าน state แล้ววาด DOM" เท่านั้น การคำนวณทั้งหมดอยู่ใน `splitter.js`
5. **ไม่มีข้อมูลผู้ใช้ออกจากเครื่อง** — ข้อมูลบิลเก็บใน `localStorage` ของเบราว์เซอร์เท่านั้น การอ่านใบเสร็จ (OCR) ก็ประมวลผลในเบราว์เซอร์ผ่าน Tesseract.js (โหลดโมดูล/โมเดลภาษาจาก CDN สาธารณะ แต่ตัวรูปภาพใบเสร็จไม่ถูกส่งขึ้นเซิร์ฟเวอร์ใด ๆ)

## โครงสร้างไฟล์

```
receipt-splitter/
├── index.html          โครงหน้าเว็บทั้งหมด (8 หน้าจอในไฟล์เดียว — ดูหัวข้อ "Flow ของหน้าจอ")
├── manifest.json        ข้อมูล PWA (ไอคอน, ชื่อแอป, สีธีม) สำหรับ "เพิ่มลงหน้าจอโฮม"
├── service-worker.js    แคชไฟล์แอปไว้ใช้งานออฟไลน์ + ทำให้ติดตั้งเป็น PWA ได้
├── css/
│   └── style.css        ธีม "Calm Utility 2026" — Warm Gray + Muted Teal (ตัวแปรสีอยู่ใน :root ด้านบนไฟล์)
├── js/
│   ├── storage.js        อ่าน/เขียน localStorage, โครงสร้างข้อมูลเริ่มต้นของบิล
│   ├── ocr.js             เรียก Tesseract.js อ่านรูปภาพ + heuristic แปลงข้อความเป็นรายการ
│   ├── splitter.js        ฟังก์ชันคำนวณล้วน ๆ (หารเท่ากัน / หารตามรายการ) ไม่แตะ DOM
│   ├── ui.js              ฟังก์ชัน render ทุกอย่างขึ้นจอ ไม่มีการคำนวณหรือแก้ state
│   └── app.js             จุดเริ่มต้น: เก็บ state, ผูก event, เรียก storage/ocr/splitter/ui
├── tests/                 ชุดเทสต์ (unit + integration) — ดูหัวข้อ "การทดสอบ" ด้านล่าง
├── package.json           ใช้เฉพาะรันเทสต์ตอนพัฒนา ไม่จำเป็นสำหรับผู้ใช้ทั่วไป
└── icons/                ไอคอนสำหรับ PWA (192px, 512px)
```

## Flow ของหน้าจอ (screen flow)

แอปเป็น **เว็บมือถือคอลัมน์เดียว** (mobile-web) ไม่มี sidebar — ทุกหน้าจออยู่ใน `index.html` เป็น `<section class="panel" data-panel="...">` และสลับกันด้วย `ui.switchTab(name)` (ซ่อน/แสดงด้วย attribute `hidden`)

โครงเป็น **4 ขั้นตอนหลัก** โดยหนึ่งขั้นตอนมีได้หลายหน้าจอ — หน้าจอในขั้นตอนเดียวกันจะโชว์เป็น **แท็บย่อย** (`#substeps`) ให้สลับกันได้ และบอกตำแหน่งว่าเป็นหน้าที่เท่าไรของขั้นตอนนั้น

| ขั้นตอน | ป้ายบนแถบ step | หน้าจอ (`data-panel`) |
|---|---|---|
| — | (ไม่มี app bar/step) | `landing` หน้าแรก |
| — | (ไม่มีแถบ step) | `scan` เลือกวิธีเพิ่มรายการ |
| 1 | รายการ | `items` รายการอาหาร → `charges` ค่าใช้จ่ายเพิ่มเติม |
| 2 | คนกิน | `people` คนที่ร่วมกิน |
| 3 | แบ่งบิล | `method` เลือกวิธีหารบิล → `assign` ใครกินอะไร *(เฉพาะโหมด itemized)* |
| 4 | สรุป | `split` สรุปผลการหารบิล |

```
landing → scan → items → charges → people → method ─┬─ (equal) ───────────→ split
                                                    └─ (itemized) → assign → split
```

- **โครงขั้นตอนทั้งหมดอยู่ที่ `STEPS` ใน `js/ui.js` ที่เดียว** — แถบ step, แท็บย่อย, ข้อความ "ขั้นตอนที่ N จาก 4", ปุ่มย้อนกลับ/ถัดไป ล้วน derive จากตารางนี้ ถ้าจะเพิ่ม/สลับหน้าจอ ให้แก้ที่นี่ที่เดียว
- **`flowFor(mode)` ทำให้ flow เป็น dynamic ตามโหมดการหาร** — `MODE_ONLY_PANELS` ระบุว่า `assign` มีเฉพาะโหมด `itemized` ดังนั้นคนที่เลือก "หารเท่ากัน" จะไม่ต้องผ่านหน้าที่ไม่มีผลต่อการคำนวณเลย (ทั้งปุ่มถัดไป, ปุ่มย้อนกลับ และแท็บย่อย รู้เรื่องนี้เองทั้งหมด)
- **`switchTab()` มี guard**: ถ้าสั่งไปหน้าที่ไม่อยู่ใน flow ของโหมดปัจจุบัน (เช่น `assign` ตอนโหมดหารเท่ากัน) จะเด้งไปหน้าแรกของขั้นตอนนั้นแทน — เรียก `ui.getCurrentTab()` ถ้าต้องรู้ว่าสุดท้ายไปโผล่ที่ไหน
- **สลับโหมดไม่ล้างค่าที่ระบุใน "ใครกินอะไร"** — `item.consumerIds` ไม่ถูกแตะ แค่ซ่อนหน้า สลับกลับมาต้องได้ของเดิม
- **ปุ่มย้อนกลับ/ถัดไปในหน้าใช้ `data-go="back|next"` ไม่ระบุชื่อหน้าปลายทาง** — resolve ตอนคลิกด้วย `ui.previousTab()`/`ui.nextTab()` **อย่า hard-code ปลายทางกลับไปใน HTML** เพราะปลายทางของขั้นตอนที่ 3 เปลี่ยนตามโหมด
- **ปุ่มย้อนกลับ/ถัดไปเป็นไอคอนล้วน ไม่มีข้อความ** (`.btn--icon` วงกลม 48px) ใช้ path ของ Material Icons ธีม Rounded จาก MUI — `ArrowBackIosRounded` / `ArrowForwardIosRounded` **ฝัง SVG ไว้เป็นค่าคงที่ `ICON_BACK`/`ICON_NEXT` ใน `ui.js` ไม่ได้โหลดจาก CDN** เพราะแอปต้องทำงานออฟไลน์ได้ ถ้าจะเพิ่มไอคอนอื่นให้ทำแบบเดียวกัน (ก๊อป path มาฝัง ไม่ใช่ลิงก์ออกไปข้างนอก)
  - **ชื่อหน้าปลายทางย้ายไปอยู่ใน `aria-label` + `title`** ที่ `renderStepNav()` เขียนให้ (เช่น `ถัดไป: ค่าใช้จ่ายเพิ่มเติม`) — ปุ่มไอคอนล้วนต้องมี accessible name เสมอ **อย่าลบ `aria-label` ออก** และ `title` ทำให้ชี้ค้างบนเดสก์ท็อปยังเห็นว่าปุ่มพาไปไหน
  - ปุ่มไอคอนไม่ยืดเต็มแถว (`flex: 0 0 auto` ทับ `.flow-actions .btn { flex: 1 }`) แถวใช้ `justify-content: space-between` ให้ย้อนกลับอยู่ซ้าย ถัดไปอยู่ขวา — ปุ่มที่ยังมีข้อความ (เช่น `#openShareBtn`) ยังยืดเต็มพื้นที่ที่เหลือตามปกติ
  - ปุ่มย้อนกลับบน app bar (`#backBtn`, "← กลับ") เป็นคนละตัว ยังมีข้อความอยู่
- **`.eyebrow` และ `.panel-recap` ใน HTML เป็นแท็กเปล่า** `renderStepNav()` เติมให้ตอน runtime — เดิม hard-code ข้อความ "ขั้นตอนที่ N จาก 4" ไว้ในทุก panel ทำให้ 2 หน้าที่อยู่ขั้นตอนเดียวกันแสดงหัวข้อเหมือนกันเป๊ะจนผู้ใช้แยกไม่ออกว่าเปลี่ยนหน้าแล้ว **อย่าเอาข้อความกลับไปใส่ใน HTML**
- `recapFor()` สร้างบรรทัดสรุปบริบทที่ยกมาจากขั้นตอนก่อน (เช่นหน้า `charges` โชว์ `รายการอาหาร 5 อย่าง · ฿620.00`) เพื่อให้รู้ว่ากำลังทำงานบนข้อมูลชุดไหน ไม่ใช่แค่รู้ว่าอยู่หน้าไหน
- **หน้า landing แสดงปุ่ม "ทำบิลเดิมต่อ" (`#resumeBtn`) เฉพาะเมื่อมีบิลค้างใน localStorage** (เช็คตอน boot ท้าย `app.js`)

## จุดที่ควรรู้เรื่อง layout

- **`.app` เป็น scroll container ของตัวเองบนจอกว้าง (`@media (min-width: 600px)`)** ผ่าน `height: calc(100vh - 48px)` + `overflow-y: auto` — จำเป็น เพราะถ้าใช้ `overflow: hidden` เฉย ๆ เพื่อตัดมุมโค้ง **`position: sticky` ของแถบบนจะพัง** (สร้าง scroll container ที่ไม่เลื่อน) ด้วยเหตุนี้ `ui.switchTab()` จึงรีเซ็ตทั้ง `window.scrollTo(0)` และ `#app.scrollTop = 0`
- **app bar + แถบ step ถูกครอบด้วย `.topbar` ที่เป็น `position: sticky; top: 0` ก้อนเดียว** ไม่ได้ sticky แยกกัน — เพื่อไม่ต้อง hard-code offset ของแถบ step (ความสูง app bar เปลี่ยนได้ตาม `env(safe-area-inset-top)` ของอุปกรณ์)
- **สีประจำคนสร้างจากลำดับใน `bill.people` แบบวนซ้ำ 8 โทน** (`toneOf(index)` ใน `ui.js` → attribute `data-tone="0..7"` บน `.avatar`, สีจริงอยู่ใน CSS) ไม่ผูกกับ `person.id` และ **ไม่ใช้ `:nth-child` แบบเดิม** ที่รองรับได้แค่ 5 คน — ชื่อกับตัวอักษรย่อยังเป็นข้อมูลหลักเสมอ สีเป็นแค่ตัวช่วยจำ

## โครงสร้างข้อมูล (data model)

```js
bill = {
  items: [{ id, name, qty, price, consumerIds: [personId, ...] }],
  people: [{ id, name }],
  settings: {
    vatEnabled,      // boolean: มี VAT หรือไม่
    vatMode,         // 'exclusive' (ราคายังไม่รวม VAT) | 'inclusive' (ราคารวม VAT แล้ว)
    vatPercent,      // ตัวเลข % เช่น 7
    serviceEnabled,  // boolean: มีค่าบริการหรือไม่
    servicePercent,  // ตัวเลข % เช่น 10
  },
}
```

- `item.consumerIds` ว่าง = รายการนั้นถือว่า "ทุกคนกินร่วมกัน" ตอนหารแบบ itemized
- **เริ่มบิลได้ 3 ทางจากหน้า `scan`**: ถ่ายรูปใบเสร็จ (`#cameraInput`), นำเข้ารูปจากคลัง (`#fileInput`) หรือกด "เพิ่มรายการเอง" (`#manualStartBtn` ใน `app.js`) ซึ่งจะข้ามไปหน้า `items` แล้วเปิด popup เพิ่มรายการให้ทันที — ไม่บังคับต้องสแกนก่อนเสมอไป
- **การเพิ่มรายการอาหารใหม่ต้องผ่าน popup เสมอ** (`#itemModalOverlay` ใน `index.html`, ควบคุมด้วย `openItemModal()`/`closeItemModal()` ใน `ui.js`) — ปุ่ม "+ เพิ่มรายการ" และ "เริ่มกรอกรายการเอง" ทั้งคู่เปิด popup นี้ ไม่ได้สร้างการ์ดเปล่าลง DOM ตรง ๆ เหมือนเวอร์ชันก่อน ปิดได้ 3 ทาง (ปุ่มยกเลิก / Escape / คลิกนอกกล่อง) ทุกทางไม่บันทึกอะไร ต้องกด "+ เพิ่มรายการ" ในกล่องเท่านั้นจึงจะ push ลง `bill.items` จริง — **การแก้ไขรายการที่มีอยู่แล้วยังทำแบบ inline เหมือนเดิม** (แก้ในช่อง input ของการ์ดได้ตรง ๆ) popup มีผลแค่ตอน "เพิ่มใหม่" เท่านั้น
- **"เริ่มบิลใหม่" มี 2 ทางเข้า** — ปุ่ม `↺` บน app bar (`#restartBtn`) และปุ่มในหน้าต่างแชร์ (`#newBillBtn`) ทั้งคู่เรียก `startNewBill()` ตัวเดียวกันใน `app.js` (ซึ่งปิด overlay ทั้งหมด, เคลียร์หน้าสแกน, รีเซ็ตโหมดหารเป็น `equal` แล้วพากลับไปหน้า `scan`)
- **"เริ่มบิลใหม่" เก็บ `bill.people` ไว้ ไม่รีเซ็ตตาม `createEmptyBill()`** — โค้ดจะสำรอง `bill.people` ไว้ก่อนสร้างบิลเปล่า แล้วค่อยใส่กลับเข้าไปทับ ถ้าจะเพิ่มฟิลด์ใหม่ที่ควร "เก็บไว้" เหมือนกัน ให้ทำแบบเดียวกัน (สำรองก่อน createEmptyBill แล้วค่อยใส่กลับ)
- **`renderAssignList()` ต้องถูกเรียกใน `renderAll()` เสมอ แม้โหมดหารเท่ากันจะไม่มีหน้า `assign` ใน flow** — เทสต์ integration คลิกปุ่มขั้นตอนแล้ว query `#assignList .assign-item` ตรง ๆ โดยไม่สนว่า panel นั้นมองเห็นอยู่หรือไม่ **อย่าเปลี่ยนไป render เฉพาะตอน panel active** จะทำให้เทสต์ล้มทั้งชุด
- **ปุ่ม "✅ ทุกคน" ในหน้าระบุคนกิน** (`assignHandlers.onSelectAllConsumers` ใน `app.js`) ตั้ง `item.consumerIds` เป็นรายชื่อทุกคนตรง ๆ (ไม่ใช่แค่ปล่อยว่างแบบ implicit) เพื่อให้เห็นสถานะ "เลือกทุกคนแล้ว" ชัดเจนบน UI และยังกดถอนเป็นรายคนได้ตามปกติหลังจากนั้น
- **สรุป "ใครกินอะไรบ้าง" บนหน้าหารบิล** ใช้ `getConsumptionSummary()` (pure function ใน `splitter.js`) แยกจากการคำนวณเงินโดยเจตนา — แสดงผลเหมือนกันไม่ว่าจะเลือกโหมดหารเท่ากันหรือหารตามรายการ เพราะเป็นข้อมูล "ใครกินอะไร" ไม่ใช่ตัวเลขที่ต้องจ่าย ตอนนี้แต่ละรายการที่คืนมามี `lineTotal`/`sharedWith`/`share`/`isShared` ติดมาด้วย เพื่อให้หน้ารายละเอียดแสดงส่วนแบ่งต่อรายการได้โดยไม่ต้องคำนวณซ้ำใน `ui.js`
- **แตะการ์ดคนในหน้าสรุป (`.split-card`) จะเปิด bottom sheet รายละเอียด** (`ui.openPersonSheet(bill, mode, personId)` → `#personSheetOverlay`) แสดงรายการอาหารที่คนนั้นหารทีละบรรทัดพร้อมส่วนแบ่งเป็นเงิน การ์ดบนหน้าสรุปโชว์ chip ได้สูงสุด `MAX_CHIPS` (10) ที่เหลือยุบเป็น `+N` — รายละเอียดทั้งหมดดูใน sheet
  - **โหมด `itemized`**: ตัวเลขกระทบยอดกันตรง ๆ — `ค่าอาหาร (row.subtotal) + ส่วนแบ่งค่าบริการ/ภาษี (row.amount - row.subtotal) = row.amount` และผลรวมของ `share` ทุกรายการเท่ากับ `row.subtotal`
  - **โหมด `equal`**: ยอดมาจาก `ยอดบิล ÷ จำนวนคน` ไม่ได้คิดจากรายการที่กิน จึงต้องมีหมายเหตุกำกับว่ารายการที่แสดงเป็น "ข้อมูลอ้างอิง" เท่านั้น **ห้ามเอาผลรวม `share` ไปแสดงเป็นยอดที่ต้องจ่ายในโหมดนี้** เพราะจะไม่ตรงกับยอดจริง
- **ข้อความ "ยอดของทุกคนรวมกันตรงกับยอดบิลแล้ว" ใน `renderSummaryPanel()` คำนวณจริงทุกครั้ง** (เทียบ `Σ perPerson.amount` กับ `totals.grandTotal` ระดับสตางค์) ไม่ใช่ข้อความตายตัว — ถ้าไม่ตรงจะไม่แสดงเลย **อย่าเปลี่ยนกลับไป hard-code** เพราะมันคือตัวยืนยันผลของ `distributeRounded()`
- การคำนวณยอดทั้งบิลอยู่ใน `calcBillTotals()` (`js/splitter.js`) แบ่งเป็น 2 กรณี:
  - **exclusive** (ราคายังไม่รวม VAT หรือไม่มี VAT): `subtotal → +service% → +vat% (คิดจาก subtotal+service)`
  - **inclusive** (ราคารวม VAT แล้ว): แยกฐานราคาก่อนภาษีออกจาก `subtotal` ก่อน (`foodBase = subtotal / (1+vat%)`) แล้วค่อยคิดค่าบริการจากฐานนั้น เพื่อไม่บวก VAT ซ้ำ
- โหมด "หารตามรายการ" หาส่วนแบ่งค่าบริการ/ภาษีของแต่ละคนจาก `grandTotal - subtotal` (ไม่ใช่ serviceAmount+vatAmount ตรง ๆ) เพื่อให้ถูกต้องไม่ว่าจะเลือกโหมด VAT แบบไหน

## จุดที่ควรรู้ก่อนแก้โค้ด

- **การอ่านใบเสร็จ (OCR) เป็นการเดา ไม่ใช่ความแม่นยำ 100%** — ใบเสร็จแต่ละร้านหน้าตาต่างกันมาก `parseReceiptLines()` ใน `ocr.js` ใช้กลยุทธ์เรียงลำดับดังนี้:
  1. หาเส้นคั่นส่วน (`----`/`====`/`....`) ถ้ามี ใช้ตัด "ส่วนรายการอาหาร" ออกจากหัวร้าน/ท้ายบิลตามโครงสร้างจริง (ไม่พึ่งคำสำคัญเพียงอย่างเดียว)
  2. ถ้าเจอบรรทัด "หัวตาราง" ของลิสรายการ (เช่น "รายการ จำนวน ราคา") จะยึดคอลัมน์ตามหัวตารางนั้น (`parseItemFromColumns`)
  3. ถ้าไม่เจอหัวตาราง หรือคอลัมน์ของบรรทัดนั้นไม่ตรงกับหัวตาราง จะ fallback ไปใช้การเดาแบบเดิม (`parseItemFromFreeText`) — มองหาตัวเลขราคาท้ายบรรทัด
  4. สแกนทุกบรรทัดหายอดสรุปท้ายบิล (`extractFooterTotals`) แล้วเทียบกับยอดรวมที่พาร์สรายการได้ (`reconcileTotals`) เพื่อเตือนผู้ใช้ถ้ายอดไม่ตรงกัน
  ถ้าต้องการแม่นยำขึ้นอีก ควรปรับ `SKIP_WORDS`/`FOOTER_PATTERNS`/`HEADER_KEYWORDS` ตามรูปแบบใบเสร็จที่เจอบ่อย — **`parseReceiptLines()` คืนค่าเป็น `{ items, footerTotals, reconciliation }` ไม่ใช่ array เปล่า ๆ** ต้องเข้าถึงผ่าน `.items`
- **Tesseract อ่านภาษาไทยมักแทรกช่องว่างระหว่างตัวอักษรแทบทุกตัวเวลาอ่านพลาด** (เช่น "คิวที่" กลายเป็น
  "ค ิ ว ท ี ่") ทำให้ `SKIP_WORDS`/`FOOTER_PATTERNS` ที่ match แบบ substring/regex ตรง ๆ ไม่เจอคำเลย —
  `isSkipLine`/`matchFooterPattern` จึงเรียก `collapseThaiCharSpacing()` ก่อนเทียบเสมอ (รวมตัวอักษร
  ไทย 2 ตัวที่คั่นด้วยวรรคกลับเป็นคำเดิม) **ถ้าเพิ่มคำใหม่ลง `SKIP_WORDS`/`FOOTER_PATTERNS` ไม่ต้อง
  ทำอะไรเพิ่ม** ฟังก์ชันนี้ทำงานอยู่ก่อนแล้วในทุก path — แต่ **ห้าม** เอาไปใช้ตอนดึงชื่อ/ราคารายการ
  อาหารจริง (`parseItemFromFreeText`/`parseItemFromColumns`) เพราะจะไปรวมตัวอักษรที่ควรมีวรรค
  คั่นจริง ๆ ในชื่อเมนูเข้าด้วยกัน
- **`recognizeReceiptText()` ประมวลผลรูปก่อนเข้า Tesseract เสมอ** ผ่าน `preprocessReceiptImage()`
  (ขยายรูปเล็ก, แปลง grayscale, binarize ด้วย Otsu's method — ปรับ threshold อัตโนมัติตามภาพ ไม่
  hardcode) ก่อนส่งเข้า `worker.recognize()` ที่ตั้ง `tessedit_pageseg_mode = PSM.SINGLE_BLOCK` และ
  `OEM.LSTM_ONLY` ไว้ (ผ่าน `createWorker()` เพราะ `Tesseract.recognize()` ตัวช่วยแบบเดิม deprecate
  ไปแล้วและไม่รองรับการปรับ parameter เหล่านี้) ถ้า preprocessing ล้ม (เบราว์เซอร์เก่าไม่รองรับ
  `createImageBitmap`) จะ fallback ไปอ่านไฟล์ต้นฉบับตรง ๆ โดยไม่บล็อกฟีเจอร์ — ตรรกะคำนวณ threshold
  (`computeOtsuThreshold`) แยกเป็น pure function ต่างหาก มีเทสต์ตรง ๆ ใน `tests/ocr.test.js` ส่วน
  preprocessing ที่พึ่ง `<canvas>`/`Image` จริงต้องทดสอบด้วยตา (ดู README/SKILL.md หัวข้อทดสอบ)
- **ห้ามใส่ business logic ใน ui.js** — ถ้าต้องคำนวณอะไรใหม่ ให้เขียนฟังก์ชัน pure function เพิ่มใน `splitter.js` แล้วเรียกใช้จาก `ui.js`/`app.js`
- **รายการอาหารแสดงเป็นการ์ด ไม่ใช่ตาราง** (`.item-card` ใน `ui.js`) เพื่อให้กดง่ายบนมือถือ มีปุ่ม +/- ปรับจำนวนแบบแตะ — การแก้ input ในการ์ด (`updateRowSum` ใน `app.js`) ตั้งใจไม่ re-render การ์ดทั้งหมดตอนพิมพ์ เพื่อไม่ให้ cursor กระโดด ถ้าจะแก้ ระวังจุดนี้
- **ทดสอบการคำนวณได้ด้วย Node โดยตรง** เพราะ `splitter.js` และ `ocr.js` เป็น pure ES module ไม่พึ่ง DOM เช่น:
  ```bash
  node --input-type=module -e "import {splitEqual} from './js/splitter.js'; console.log(splitEqual(...))"
  ```

## การนำไปใช้งานจริง (deployment)

ไม่ต้องมีเซิร์ฟเวอร์ของตัวเอง แต่การ "เพิ่มลงหน้าจอโฮม" (PWA) ต้องเปิดผ่าน HTTPS (ยกเว้น `localhost`) ตามข้อกำหนดของเบราว์เซอร์ วิธีที่ง่ายที่สุดคือฝากไฟล์ทั้งโฟลเดอร์นี้ไว้บน static hosting ฟรี เช่น GitHub Pages — ดูรายละเอียดใน `README.md`

- **ทุกครั้งที่แก้ไฟล์ที่อยู่ใน `APP_SHELL`** (`service-worker.js`) **ต้องเพิ่มเลขเวอร์ชันใน `CACHE_NAME`** (เช่น `v1` → `v2`) ไม่งั้นผู้ใช้ที่ติดตั้งแอปเป็น PWA ไว้แล้วจะเห็นเวอร์ชันแคชเก่าค้างอยู่ ไม่ได้อัปเดตโค้ดใหม่ทันที
- **PWA (manifest + service worker) ใช้งานไม่ได้บน `file://`** — ต้องเปิดผ่าน HTTP/HTTPS จริง (เช่น GitHub Pages, หรือ `python -m http.server` ตอนพัฒนา) จึงจะเห็นพฤติกรรม "เพิ่มลงหน้าจอโฮม" และ service worker ทำงาน — ถ้าเปิดไฟล์ตรง ๆ จาก disk ฟีเจอร์นี้จะไม่แสดงผล (ไม่ใช่บั๊ก เป็นข้อจำกัดของเบราว์เซอร์เอง)

## การทดสอบ (tests/)

รันด้วย `npm install && npm test` (ใช้ Node.js built-in test runner `node:test` — ไม่ผูกกับ framework ทดสอบภายนอกใด ๆ เพื่อให้ตรงกับหลักการ "ไม่มี build step" ของโปรเจกต์)

> **ต้องใช้ Node.js 20 ขึ้นไป** — `jsdom` เวอร์ชันปัจจุบันมี dependency (`html-encoding-sniffer` → `@exodus/bytes`) ที่เป็น ESM ล้วน ถ้ารันบน Node 18 จะได้ `ERR_REQUIRE_ESM` และ `tests/integration.test.js` **ทั้งไฟล์** จะ fail ตั้งแต่ตอน import ก่อนแตะโค้ดแอปเลยแม้แต่บรรทัดเดียว (อีก 3 ไฟล์ยังผ่านปกติเพราะไม่ใช้ jsdom) — ถ้าเจอ error นี้ให้เช็ค `node -v` ก่อนสงสัยว่าโค้ดแอปพัง

- `tests/splitter.test.js` — เทสต์การคำนวณล้วน ๆ ครบทั้ง 4 สถานการณ์ (ไม่มี VAT/ไม่มีค่าบริการ, VAT แบบบวกเพิ่ม, VAT แบบรวมแล้ว, ผสมกับค่าบริการ) รวมถึง edge case (คนว่าง, รายการว่าง, ไม่มีใครกินสักรายการ)
- `tests/ocr.test.js` — เทสต์ heuristic แปลงข้อความ OCR เป็นรายการ (การดึง qty, การข้ามบรรทัดยอดรวม/ภาษี, ตัวเลขที่ไม่สมเหตุสมผล)
- `tests/storage.test.js` — เทสต์ localStorage และการ migrate ข้อมูลบิลเวอร์ชันเก่า (stub `localStorage`/`window.crypto` เป็น in-memory เพราะ Node ไม่มี browser API เหล่านี้จริง)
- `tests/integration.test.js` — **เทสต์แบบ end-to-end ที่รันโค้ดจริงของแอป** (`js/app.js` ตัวเดียวกับที่ผู้ใช้ใช้จริง ไม่ได้เขียน mock/reimplement) บน DOM ที่สร้างจาก `index.html` ตัวจริงผ่าน [jsdom](https://github.com/jsdom/jsdom) แล้วจำลองการกด/พิมพ์ของผู้ใช้ทั้งวงจร (วางข้อความ OCR → แปลงเป็นรายการ → เพิ่มรายการ/คนเอง → เปิด VAT/ค่าบริการ → ดูผลหารบิลทั้ง 2 โหมด → เริ่มบิลใหม่) ตรวจว่าตัวเลขที่แสดงบนจอและข้อมูลที่บันทึกลง localStorage ถูกต้องจริง

**ข้อควรรู้ตอนแก้ไข `tests/integration.test.js`:** เพราะ `js/app.js` ผูก event listener ไว้ที่ระดับบนของไฟล์ (top-level, ไม่ได้ห่อในฟังก์ชัน `init()`) โค้ดเทสต์จึงต้อง `import()` แบบ dynamic พร้อม query string กัน cache (เช่น `?fresh=${Date.now()}`) ทุกครั้งที่ต้องการ "รีสตาร์ทแอป" ในเทสต์ใหม่ ไม่งั้น state จาก test ก่อนหน้าจะค้างข้ามกัน และต้องตั้งค่า `global.document`/`global.localStorage`/`global.navigator` (ผ่าน jsdom) ให้พร้อม **ก่อน** import เสมอ เพราะโค้ดใน app.js เรียก `document.querySelector(...)` ทันทีตอนโมดูลถูกโหลด

## แนวทางต่อยอดในอนาคต (ไม่ได้ทำในเวอร์ชันนี้)

- บันทึกหลายบิลพร้อมประวัติ (ตอนนี้เก็บได้แค่บิลปัจจุบันบิลเดียว)
- Export ผลหารบิลเป็นรูปภาพ/ข้อความเพื่อแชร์ผ่าน LINE
- ปรับปรุงความแม่นยำ OCR ด้วยการครอปเฉพาะส่วนตารางรายการก่อนส่งเข้า Tesseract (ตอนนี้ทำแค่
  grayscale + binarize เต็มรูปเท่านั้น ยังไม่ครอป)
- **พิจารณาแล้วแต่ไม่ทำ: สลับไปใช้ `tessdata_best` แทน `tessdata_fast`** — `tessdata.projectnaptha.com`
  ที่ tutorial เก่ามักแนะนำเลิกดูแลแล้ว ส่วน CDN ที่ยังใช้งานได้ (jsDelivr ผ่าน `@tesseract.js-data/{lang}`)
  ยังไม่พบว่ามีตัวแปร `best` ให้ใช้ในรูปแบบ `.traineddata.gz` ที่ Tesseract.js ต้องการ การจะใช้ต้อง
  host ไฟล์เอง ขัดหลักการ "ไม่มีเซิร์ฟเวอร์ของตัวเอง" ข้อ 5 ด้านบน และความแม่นยำที่ต่างจาก fast มี
  ไม่ถึง 5% (ตามข้อมูลจาก Tesseract maintainers) — ถ้าจะทำจริงต้องหา CDN ที่เชื่อถือได้ก่อน
