# SKILL.md — หารบิล (Receipt Splitter)

เอกสารนี้อธิบายโครงสร้างและแนวคิดของโปรเจกต์ สำหรับใช้อ้างอิงเวลากลับมาแก้ไข/ต่อยอดโค้ดในอนาคต (ทั้งสำหรับคนและสำหรับ Claude หรือ AI ตัวอื่นที่ช่วยแก้โค้ด)

## ภาพรวม

เว็บแอปสำหรับสแกนใบเสร็จร้านอาหารด้วย OCR แล้วหารค่าใช้จ่ายกับเพื่อน ทำงานฝั่งไคลเอนต์ (client-side) ทั้งหมด **ไม่มีเว็บเซิร์ฟเวอร์ของตัวเอง** — เปิดจากไฟล์ `index.html` ได้โดยตรง หรือฝากไว้บน static hosting ใดก็ได้ (GitHub Pages, Netlify, Cloudflare Pages ฯลฯ) และรองรับการ "เพิ่มลงหน้าจอโฮม" บนมือถือผ่าน PWA manifest + service worker

## หลักการออกแบบ

1. **ไม่มี build step** — ใช้ vanilla JavaScript (ES Modules) ล้วน ๆ ไม่มี bundler/framework ไม่ต้อง `npm install` ก่อนใช้งาน เปิดไฟล์แล้วรันได้ทันที
2. **แยกหน้าที่ชัดเจนต่อไฟล์ (single responsibility)** — ดูหัวข้อ "โครงสร้างไฟล์" ด้านล่าง
3. **State เดียว ความจริงเดียว (single source of truth)** — ตัวแปร `bill` ใน `js/app.js` คือสถานะทั้งหมดของบิลปัจจุบัน ทุกการเปลี่ยนแปลงต้องผ่าน handler ใน `app.js` แล้วเรียก `persist()` + เรียก `ui.render*()` ที่เกี่ยวข้องเสมอ
4. **ui.js ไม่มี business logic** — มีหน้าที่ "อ่าน state แล้ววาด DOM" เท่านั้น การคำนวณทั้งหมดอยู่ใน `splitter.js`
5. **ไม่มีข้อมูลผู้ใช้ออกจากเครื่อง** — ข้อมูลบิลเก็บใน `localStorage` ของเบราว์เซอร์เท่านั้น การอ่านใบเสร็จ (OCR) ก็ประมวลผลในเบราว์เซอร์ผ่าน Tesseract.js (โหลดโมดูล/โมเดลภาษาจาก CDN สาธารณะ แต่ตัวรูปภาพใบเสร็จไม่ถูกส่งขึ้นเซิร์ฟเวอร์ใด ๆ)

## โครงสร้างไฟล์

```
receipt-splitter/
├── index.html          โครงหน้าเว็บทั้งหมด (4 แท็บ: สแกน / รายการ / คนกิน / หารบิล)
├── manifest.json        ข้อมูล PWA (ไอคอน, ชื่อแอป, สีธีม) สำหรับ "เพิ่มลงหน้าจอโฮม"
├── service-worker.js    แคชไฟล์แอปไว้ใช้งานออฟไลน์ + ทำให้ติดตั้งเป็น PWA ได้
├── css/
│   └── style.css        ธีมสีครีม-ส้ม-ขาว (cream/orange/white) ทั้งหมด (ตัวแปรสีอยู่ใน :root ด้านบนไฟล์)
├── js/
│   ├── storage.js        อ่าน/เขียน localStorage, โครงสร้างข้อมูลเริ่มต้นของบิล
│   ├── ocr.js             เรียก Tesseract.js อ่านรูปภาพ + heuristic แปลงข้อความเป็นรายการ
│   ├── splitter.js        ฟังก์ชันคำนวณล้วน ๆ (หารเท่ากัน / หารตามรายการ) ไม่แตะ DOM
│   ├── ui.js              ฟังก์ชัน render ทุกอย่างขึ้นจอ ไม่มีการคำนวณหรือแก้ state
│   └── app.js             จุดเริ่มต้น: เก็บ state, ผูก event, เรียก storage/ocr/splitter/ui
├── tests/                 ชุดเทสต์ (unit + integration) — ดูหัวข้อ "การทดสอบ" ด้านล่าง
├── package.json           ใช้เฉพาะรันเทสต์ตอนพัฒนา ไม่จำเป็นสำหรับผู้ใช้ทั่วไป
└── icons/                ไอคอนสำหรับ PWA (192px, 512px)
```

## โครงสร้างข้อมูล (data model)

```js
bill = {
  categories: [{ id, name }],
  items: [{ id, name, qty, price, categoryId, consumerIds: [personId, ...] }],
  people: [{ id, name }],
  settings: {
    vatEnabled,      // boolean: มี VAT หรือไม่
    vatMode,         // 'exclusive' (ราคายังไม่รวม VAT) | 'inclusive' (ราคารวม VAT แล้ว)
    vatPercent,      // ตัวเลข % เช่น 7
    serviceEnabled,  // boolean: มีค่าบริการหรือไม่
    servicePercent,  // ตัวเลข % เช่น 10
  },
}
```

- `item.consumerIds` ว่าง = รายการนั้นถือว่า "ทุกคนกินร่วมกัน" ตอนหารแบบ itemized
- **เริ่มบิลได้ 2 ทางจากแท็บแรก ("เริ่มต้น")**: สแกนใบเสร็จ (OCR) หรือกดปุ่ม "เริ่มกรอกรายการเอง" (`#manualStartBtn` ใน `app.js`) ซึ่งจะเพิ่มรายการเปล่า 1 รายการให้พร้อมกรอกทันทีแล้วสลับไปแท็บ "รายการ" — ไม่บังคับต้องสแกนก่อนเสมอไป
- การคำนวณยอดทั้งบิลอยู่ใน `calcBillTotals()` (`js/splitter.js`) แบ่งเป็น 2 กรณี:
  - **exclusive** (ราคายังไม่รวม VAT หรือไม่มี VAT): `subtotal → +service% → +vat% (คิดจาก subtotal+service)`
  - **inclusive** (ราคารวม VAT แล้ว): แยกฐานราคาก่อนภาษีออกจาก `subtotal` ก่อน (`foodBase = subtotal / (1+vat%)`) แล้วค่อยคิดค่าบริการจากฐานนั้น เพื่อไม่บวก VAT ซ้ำ
- โหมด "หารตามรายการ" หาส่วนแบ่งค่าบริการ/ภาษีของแต่ละคนจาก `grandTotal - subtotal` (ไม่ใช่ serviceAmount+vatAmount ตรง ๆ) เพื่อให้ถูกต้องไม่ว่าจะเลือกโหมด VAT แบบไหน

## จุดที่ควรรู้ก่อนแก้โค้ด

- **การอ่านใบเสร็จ (OCR) เป็นการเดา ไม่ใช่ความแม่นยำ 100%** — ใบเสร็จแต่ละร้านหน้าตาต่างกันมาก ฟังก์ชัน `parseReceiptLines()` ใน `ocr.js` ใช้ regex เดาว่าบรรทัดไหนคือ "ชื่อ...ราคา" เท่านั้น ถ้าต้องการแม่นยำขึ้น ควรปรับ `skipWords` (คำที่ควรข้าม เช่น "รวม", "ภาษี") หรือปรับ `priceRegex` ตามรูปแบบใบเสร็จที่เจอบ่อย
- **ห้ามใส่ business logic ใน ui.js** — ถ้าต้องคำนวณอะไรใหม่ ให้เขียนฟังก์ชัน pure function เพิ่มใน `splitter.js` แล้วเรียกใช้จาก `ui.js`/`app.js`
- **รายการอาหารแสดงเป็นการ์ด ไม่ใช่ตาราง** (`.item-card` ใน `ui.js`) เพื่อให้กดง่ายบนมือถือ มีปุ่ม +/- ปรับจำนวนแบบแตะ — การแก้ input ในการ์ด (`updateRowSum` ใน `app.js`) ตั้งใจไม่ re-render การ์ดทั้งหมดตอนพิมพ์ เพื่อไม่ให้ cursor กระโดด ถ้าจะแก้ ระวังจุดนี้
- **ทดสอบการคำนวณได้ด้วย Node โดยตรง** เพราะ `splitter.js` และ `ocr.js` เป็น pure ES module ไม่พึ่ง DOM เช่น:
  ```bash
  node --input-type=module -e "import {splitEqual} from './js/splitter.js'; console.log(splitEqual(...))"
  ```

## การนำไปใช้งานจริง (deployment)

ไม่ต้องมีเซิร์ฟเวอร์ของตัวเอง แต่การ "เพิ่มลงหน้าจอโฮม" (PWA) ต้องเปิดผ่าน HTTPS (ยกเว้น `localhost`) ตามข้อกำหนดของเบราว์เซอร์ วิธีที่ง่ายที่สุดคือฝากไฟล์ทั้งโฟลเดอร์นี้ไว้บน static hosting ฟรี เช่น GitHub Pages — ดูรายละเอียดใน `README.md`

- **ทุกครั้งที่แก้ไฟล์ที่อยู่ใน `APP_SHELL`** (`service-worker.js`) **ต้องเพิ่มเลขเวอร์ชันใน `CACHE_NAME`** (เช่น `v1` → `v2`) ไม่งั้นผู้ใช้ที่ติดตั้งแอปเป็น PWA ไว้แล้วจะเห็นเวอร์ชันแคชเก่าค้างอยู่ ไม่ได้อัปเดตโค้ดใหม่ทันที
- **PWA (manifest + service worker) ใช้งานไม่ได้บน `file://`** — ต้องเปิดผ่าน HTTP/HTTPS จริง (เช่น GitHub Pages, หรือ `python -m http.server` ตอนพัฒนา) จึงจะเห็นพฤติกรรม "เพิ่มลงหน้าจอโฮม" และ service worker ทำงาน — ถ้าเปิดไฟล์ตรง ๆ จาก disk ฟีเจอร์นี้จะไม่แสดงผล (ไม่ใช่บั๊ก เป็นข้อจำกัดของเบราว์เซอร์เอง)

## การทดสอบ (tests/)

รันด้วย `npm install && npm test` (ใช้ Node.js built-in test runner `node:test` — ไม่ผูกกับ framework ทดสอบภายนอกใด ๆ เพื่อให้ตรงกับหลักการ "ไม่มี build step" ของโปรเจกต์)

- `tests/splitter.test.js` — เทสต์การคำนวณล้วน ๆ ครบทั้ง 4 สถานการณ์ (ไม่มี VAT/ไม่มีค่าบริการ, VAT แบบบวกเพิ่ม, VAT แบบรวมแล้ว, ผสมกับค่าบริการ) รวมถึง edge case (คนว่าง, รายการว่าง, ไม่มีใครกินสักรายการ)
- `tests/ocr.test.js` — เทสต์ heuristic แปลงข้อความ OCR เป็นรายการ (การดึง qty, การข้ามบรรทัดยอดรวม/ภาษี, ตัวเลขที่ไม่สมเหตุสมผล)
- `tests/storage.test.js` — เทสต์ localStorage และการ migrate ข้อมูลบิลเวอร์ชันเก่า (stub `localStorage`/`window.crypto` เป็น in-memory เพราะ Node ไม่มี browser API เหล่านี้จริง)
- `tests/integration.test.js` — **เทสต์แบบ end-to-end ที่รันโค้ดจริงของแอป** (`js/app.js` ตัวเดียวกับที่ผู้ใช้ใช้จริง ไม่ได้เขียน mock/reimplement) บน DOM ที่สร้างจาก `index.html` ตัวจริงผ่าน [jsdom](https://github.com/jsdom/jsdom) แล้วจำลองการกด/พิมพ์ของผู้ใช้ทั้งวงจร (วางข้อความ OCR → แปลงเป็นรายการ → เพิ่มรายการ/คนเอง → เปิด VAT/ค่าบริการ → ดูผลหารบิลทั้ง 2 โหมด → เริ่มบิลใหม่) ตรวจว่าตัวเลขที่แสดงบนจอและข้อมูลที่บันทึกลง localStorage ถูกต้องจริง

**ข้อควรรู้ตอนแก้ไข `tests/integration.test.js`:** เพราะ `js/app.js` ผูก event listener ไว้ที่ระดับบนของไฟล์ (top-level, ไม่ได้ห่อในฟังก์ชัน `init()`) โค้ดเทสต์จึงต้อง `import()` แบบ dynamic พร้อม query string กัน cache (เช่น `?fresh=${Date.now()}`) ทุกครั้งที่ต้องการ "รีสตาร์ทแอป" ในเทสต์ใหม่ ไม่งั้น state จาก test ก่อนหน้าจะค้างข้ามกัน และต้องตั้งค่า `global.document`/`global.localStorage`/`global.navigator` (ผ่าน jsdom) ให้พร้อม **ก่อน** import เสมอ เพราะโค้ดใน app.js เรียก `document.querySelector(...)` ทันทีตอนโมดูลถูกโหลด

## แนวทางต่อยอดในอนาคต (ไม่ได้ทำในเวอร์ชันนี้)

- บันทึกหลายบิลพร้อมประวัติ (ตอนนี้เก็บได้แค่บิลปัจจุบันบิลเดียว)
- Export ผลหารบิลเป็นรูปภาพ/ข้อความเพื่อแชร์ผ่าน LINE
- ปรับปรุงความแม่นยำ OCR ด้วยการครอปเฉพาะส่วนตารางรายการก่อนส่งเข้า Tesseract
